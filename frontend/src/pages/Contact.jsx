import React from "react";
import { Typography } from "@mui/material";

const Contacts = () => {
  return (
    <Typography variant="h5" sx={{ mt: "100px" }}>
      GET IN TOUCH
    </Typography>
  );
};
export default Contacts;
